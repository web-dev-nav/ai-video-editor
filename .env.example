# Copy to .env (install.sh does this) or set these in the GUI via "API keys".
# Only the providers you use are needed. Never commit .env.

# AI editor — Claude (Anthropic)
ANTHROPIC_API_KEY=

# AI editor — ChatGPT (OpenAI)
OPENAI_API_KEY=

# AI editor via OpenRouter, and the Smart hook / YouTube chapters extras
OPENROUTER_API_KEY=

# Optional: GUI port (default 8765)
# AVE_GUI_PORT=8765
